import { OrderCompletePipe } from './order-complete.pipe';

describe('OrderCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
