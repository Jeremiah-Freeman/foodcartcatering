import { OrderSortPipe } from './order-sort.pipe';

describe('OrderSortPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSortPipe();
    expect(pipe).toBeTruthy();
  });
});
