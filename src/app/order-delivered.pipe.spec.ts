import { OrderDeliveredPipe } from './order-delivered.pipe';

describe('OrderDeliveredPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDeliveredPipe();
    expect(pipe).toBeTruthy();
  });
});
